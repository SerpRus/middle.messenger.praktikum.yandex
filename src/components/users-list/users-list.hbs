<ul class="{{ classes }} users-list">
    {{#each users}}
        {{{TextWithAction
            login=login
            id=this.id
            iconName=../iconName
            iconTheme=../iconTheme
            eventBusName=../eventBusName
        }}}
    {{/each}}
</ul>
