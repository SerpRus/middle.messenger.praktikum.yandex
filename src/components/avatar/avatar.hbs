{{#Formfield
    id=this.id
    name=this.name
    thema=this.thema
    type=this.type
    classes=this.classes
    label=this.label
    disabled=this.disabled
    onInput=this.onInput
}}
    <img class="avatar__img" src="{{this.avatarSrc}}">
{{/Formfield}}
