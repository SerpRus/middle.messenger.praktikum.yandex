<input id="{{id}}"
       class="{{classes}} input"
       name="{{name}}"
       type="{{type}}"
       {{#if value}}value="{{value}}"{{/if}}
       placeholder="{{placeholder}}"
       {{#if disabled}}disabled{{/if}}
>
